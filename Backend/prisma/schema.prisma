// prisma/schema.prisma

datasource db {
  provider = "mysql" // 👈 여기를 반드시 mysql로 확인!
}

generator client {
  provider = "prisma-client-js"
}

// 📌 [설계도 작성] 어떤 데이터를 저장할지 정의합니다.
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}